services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    container_name: snl-backend
    restart: unless-stopped
    # ← SUPPRIMER les lignes ports complètement
    environment:
      - PORT=3001
      - NODE_ENV=${NODE_ENV:-production}
      - FRONTEND_URL=${FRONTEND_URL:-*}
      - SOCKET_SECRET=${SOCKET_SECRET}
      - JWT_SECRET=${JWT_SECRET:-change-me-in-production}
      - DATABASE_URL=${DATABASE_URL:-}
      - LOG_LEVEL=${LOG_LEVEL:-info}
    volumes:
      - snl_logs:/app/logs
    # Rattache le backend au reseau `coolify` pour joindre Postgres/Supabase
    # (sinon il reste isole sur son propre reseau et ne peut pas atteindre la DB)
    networks:
      - coolify
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:3001/health"]
      interval: 30s
      timeout: 10s
      start_period: 20s
      retries: 3

volumes:
  snl_logs:

networks:
  coolify:
    external: true